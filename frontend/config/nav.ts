import { DashboardConfig } from "@/types"

export const navConfig: DashboardConfig = {
    landingNav:[
        {
            title: "Home",
            href: "/",
        }

    ],

  mainNav: [
    {
      title: "Profile",
      href: "/profile",
    }
  ],
}